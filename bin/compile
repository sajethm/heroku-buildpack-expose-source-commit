#!/bin/sh

# Syntax bin/compile <build-dir> <cache-dir>

BUILD_DIR=$1

echo "=====> Printing source version ($SOURCE_VERSION)"

if [ -d "$env_dir" ]; then
    for e in $(ls $BUILD_DIR); do
        echo "=====> $e"
    done
fi